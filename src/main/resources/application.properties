# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/erp_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.defer-datasource-initialization=false

# Data Initialization - Spring Boot 기본 초기화 비활성화
spring.sql.init.mode=never
spring.sql.init.continue-on-error=true

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=your_jwt_secret_key_here_make_it_long_and_secure_at_least_256_bits
jwt.expiration=86400000

# Logging Configuration
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.erp_project.erp_project.config.DataInitializer=DEBUG
logging.level.org.springframework.jdbc=DEBUG

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Toss Payments API 설정
toss.secret-key=test_sk_0RnYX2w5327K0axkv2WkVNeyqApQ
toss.client-key=test_ck_5OWRapdA8dwJmlAoQRmnro1zEqZK
toss.api-url=https://api.tosspayments.com/v1

# Redirect URLs
toss.success-url=http://localhost:8080/payments/success
toss.fail-url=http://localhost:8080/payments/fail

# 로그 레벨
logging.level.com.example.tosspayments=DEBUG

# 보안 설정
security.secret.key=your_secret_key_here_change_in_production
security.hash.validity.period=300000

# 파일 업로드 설정
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB

# 파일 저장 설정
app.file.upload-dir=uploads/notice-attachments
app.file.max-size=10485760
app.file.allowed-extensions=jpg,jpeg,png,gif,webp,pdf,doc,docx,xls,xlsx,ppt,pptx,txt,mp4,mp3,zip